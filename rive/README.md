Rive static atlas, served at https://thebonhomme.com/rive/.
Apache-2.0. Data from RTC, STM, àVélo, BIXI under their terms.

This directory is published by `.github/workflows/publish-transit.yml` from
`LeBonhommePharma/Transit`. Do not edit the atlas here — `rsync --delete`
overwrites it on each publish. This README is apex-local and is preserved
across that sync.

`/transit/` is a 200 redirect alias for old bookmarks. Rollback snapshot:
`transitA/`.
